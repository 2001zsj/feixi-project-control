# V0.8.63 代码、数据与 UI 审计

审计基线：`f277c7f0d4d05ed3a10f93304bf2f7b3f54e002b`，GitHub main；开始于 2026-09-20，继续于 2026-09-21。范围：完整单文件应用、快照、交接文档、独立浏览器测试数据。未读取用户生产浏览器 localStorage，不能认定模拟复现就是用户实际数据已损坏。

本报告先记录问题，再实施小范围修复。现已完成本地修复和验证，详见末尾结果。当前没有提交或部署。

**2026-09-21 P0补充结论：D3已在本地修复。** 历史preset不再直接修改实际缓存，而在副本上生成候选；人工字段保留，已知错误值精确纠正，差异可见并留档。新增14项专项与9组浏览器检查通过。下文初轮问题与失败证据保留，最新状态以末尾“D3闭环”节为准。

## P0：数据错误、丢失、串站

### D1 损坏缓存被自动替换；保存失败静默
- 位置：原 `index.html:525` load、`:558` save。
- 触发：KEY 内容不是合法 JSON；或浏览器配额满/禁止存储。
- 影响：load 用 SEED 替换后立即写回，原文丢失；save 吞掉错误，按钮继续提示成功。
- 是否发生：用真实 load/save + 隔离存储复现；用户生产数据是否受影响未知。
- 建议：加载失败停止写入并给出恢复提示；升级前保留原始备份；保存失败阻断成功提示。多标签并发也应检测旧快照覆盖。

### D2 合并丢字段，匹配未保证一对一
- 位置：原 `index.html:29-53` mergeLatestBase。
- 触发：BASE_VERSION 变化，旧站有 keep 之外字段；或 A 的固定 ID 匹配后，A 的需求名恰好也匹配 B。
- 影响：历史 manualNodeOverdue/nodeOverdueReasonManual 等消失；同一旧记录可进入两条站点。重复订单本身已有双侧唯一校验，但不能防止 ID 与名称回退重复消费同一记录。
- 是否发生：模拟回归已复现。交接文档记载华一路站历史串站；本轮实际种子映射正确，未证实当前再次串站。
- 建议：保留未知字段；固定 ID 优先且每个旧记录只消费一次；明确同名须唯一；重复 ID/歧义/未匹配旧记录停止合并，保留原始记录而不是猜测。

### D3 历史 preset 可覆盖后续人工变更（需方案确认）
- 位置：applySelectionPendingCorrectionPreset、applySelectionSourceContentPreset、applyConstructionProblemPreset 及旧版 apply* 链。
- 触发：旧缓存缺少对应版本标记，却已有用户补录编码、最新进展、问题状态或施工节点。
- 影响：待立项纠正会清空编码；内容初始化无条件覆盖进展/问题；时间戳无法准确区分已确认错误与较新的手工值。成功重复加载幂等不代表第一次升级安全。
- 是否发生：模拟旧缓存已复现“新编码被清空”和“手工进展被覆盖”，见 pending-migration-observations.json；未接触用户实际缓存。
- 建议：先逐项建立纠错依据、适用旧值、字段级冲突策略；保留迁移前原文及冲突记录。不可一次替换全部 preset，也不可一概“旧值优先”从而阻止已确认纠错。本轮不猜测业务优先级。

## P1：业务逻辑

### B1 模块判定仍额外依赖 selectionDone
- 位置：renderHome、renderList、renderStation、due、mixedCols。
- 触发：选址站直接补项目编码，或完工站遗留 selectionDone=false；清除项目编码但保留立项日期。
- 影响：有编码仍留选址；完工同时进入选址；待立项展示施工时间。违反编码决定立项、完工终态规则。
- 是否发生：隔离数据复现；75站当前默认分类仍正确。
- 建议：统一分类入口，完工优先、项目编码次之，不改站点业务数据凑数。

### B2 工作日反向计算边界和未知年度节点提示
- 位置：constructionDaysBetween、scheduleMeta、constructionHtml、parseDate。
- 触发：截止后遇非工作日；48工作日跨未知年度；非法日期 2026-02-31。
- 影响：反向计数把截止日再次计入，周末凭空多算逾期工作日；缺年历时自然日节点超期一并消失；详情出现 null 或虚构第1天；非法日期自动滚到3月。
- 是否发生：独立测试复现。指定的两例48工作日截止、调休日、包含立项当天的算法通过。
- 建议：反向差值与正向反对称；保持自然日节点独立；空值明确显示待计算；严格日期解析。
- 待确认：截止后的非工作日是否立即显示“已超过截止日期”而工作日超期仍为0，还是等下一个工作日才显示“总工期逾期”。不自行改变这一业务用词口径。

### B3 浇筑与装塔同日可保存
- 位置：bindConstruction/saveNode。
- 触发：录入装塔早于养护结束点确认，或反向修改浇筑日期与已有装塔同日。
- 影响：违反明确禁止同日的规则；早于养护的确认不能代替同日禁止。
- 是否发生：真实处理器模拟复现；未发现种子中已有同日记录。
- 建议：两种编辑方向均校验，仍允许进场与装塔同日。

### B4 正常/疑难更新时间互相污染与筛选误报
- 位置：lastFollow、toggleDifficult、saveProblem、saveProgress、renderList recent。
- 触发：在一类更新后切换另一类；无更新且关闭提醒的正常站筛选最近3天。
- 影响：共用更新时间掩盖另一类长期未更新；旧已解决问题被切换强制改为未解决；未跟进也出现在“最近已更新”。
- 是否发生：recent 模拟已复现；切换路径已用实际浏览器验证。修复后两个字段、状态与各自更新时间均保留。
- 建议：分别保存进展与问题更新时间，旧共享时间保留；切换不重置内容和状态；recent 按真实更新时间判断。
- 待确认：旧共享时间无法反推属于哪一类，不编造历史时间。

### B5 导出与网页口径不一致
- 位置：exportXls pendingCols、afterCols、statusTone。
- 触发：导出待立项、施工中。
- 影响：待立项固定标签而不是实际状态；施工问题及原因、节点提醒、整体工期风险没有导出；通用“逾期”着色无法区分黄色节点和红色总工期。
- 是否发生：原实现明确缺列；已验证修复后的实际下载 XLSX，字段口径和格式检查通过。
- 建议：共用网页状态函数，补齐已有业务字段，按列语义着色，保留原有打印/边框/冻结/字体。

## P2：UI / UX

### U1 移动端隐藏全部关键状态，编辑网格未适配
- 位置：720px CSS .row .mobilehide、.editgrid、输入框 inline min-width。
- 触发：390px 等手机宽度。
- 影响：列表仅剩需求站名，看不到铁塔站名、阶段、进展/问题和工期；5列编辑及320px输入可能挤出屏幕。
- 是否发生：已保存真实浏览器截图 `audit-evidence/mobile-before.png`，施工中13行的关键信息不可见。
- 建议：保留紧凑布局，让核心字段在窄屏换行；编辑网格改为两列/一列。

### U2 风险信号被单标签遮挡，节点用了红色时间线
- 位置：row、homeRow、riskLabelFor、.tl.over。
- 触发：节点逾期，或施工问题与总工期同时预警。
- 影响：列表仅工期序数变色，没有原因和截止；施工问题标签优先时遮挡工期风险；节点红底违反黄色语义。
- 是否发生：当前网页/样式可直接观察。
- 建议：独立显示施工问题、节点提示、工期风险与截止；节点只用黄。

## P3：维护性与基线

### M1 文档和快照摘要陈旧
- 位置：CODEX_HANDOFF.md、PROJECT_HANDOFF.md 前半部分、JSON module_counts。
- 触发：新接手者读取摘要。
- 影响：V0.8.41、施工排程、旧12/7分类、自然日与工作日、节点解除与历史保留等互相矛盾。
- 是否发生：已确认。JSON 明细与运行是16/11/8/13/27，摘要仍16/12/7/13/27。
- 建议：报告列明冲突，以本次用户要求执行；摘要可按明细更新，不修改75条原始记录。历史条目保留但标记已被后续规则替代。

### M2 迁移链、旧排程和巨型单行函数
- 位置：apply*、renderSchedule、scheduleCols、旧CSS、renderList/renderStation/constructionHtml。
- 触发：后续继续修补版本。
- 影响：修改面大、迁移顺序隐含、旧导出通道仍可调用。#schedule 已正确路由施工中，没有恢复导航入口。
- 是否发生：结构性问题；并非每段死代码都造成当前故障。
- 建议：先保留兼容路由，移除不可达排程视图和导出分支；迁移机制另行分阶段重构，详见后续方案。

## 初步验证

真实源代码 VM 测试初轮 28 项：16通过、12失败（失败不等同12个独立缺陷）。结果在 `audit-evidence/baseline-results.json`。包含75站、分类、唯一ID、重复订单隔离、华一路映射、日期规则、缓存损坏、保存失败、迁移字段和状态边界。

已核对2025/2026年日历与国务院通知的政府转载，内置放假和调休日一致：
- [2026年通知](https://www.beijing.gov.cn/fuwu/bmfw/sy/jrts/202511/t20251104_4258838.html)
- [2025年通知](https://www.forestry.gov.cn/c/www/szxx/594663.jhtml)

未使用旧Excel、相似名称或坐标推断站点；未连接生产浏览器缓存。浏览器与模拟测试均为独立测试副本。

## 修复结果（2026-09-21）

| 问题 | 本轮处置 | 证据与边界 |
|---|---|---|
| D1 | 已修复可确定路径 | 损坏JSON/无效结构停止写回；首次加载现有缓存前备份原文；保存失败抛错并提示，阻断原来的成功提示；检测另一标签已写入的快照，避免旧页面覆盖。不是跨进程数据库事务。 |
| D2 | 已修复 | 合并完整保留旧字段；固定ID优先，精确名称或双侧唯一订单回退；旧记录只使用一次；无法明确匹配时停止，不删原始缓存。 |
| D3 | 已在后续授权中修复 | 使用私有候选生成器与字段保护层；未知差异保留人工值并显示冲突，只有列明的错误旧值自动纠正；详见D3闭环。 |
| B1 | 已修复 | stationModule 统一模块归属；完工优先、编码次之；详情/筛选/工作台/混合导出使用一致判定，不改75条种子状态。 |
| B2 | 明确计算问题已修复 | 反向区间对称；非法日期拒绝；缺日历仍保留自然日节点提醒；不显示null或假第1天；截止后非工作日的“逾期”措辞仍待确认。 |
| B3 | 已修复 | 双向拒绝浇筑/装塔同日，保留进场/装塔同日许可。 |
| B4 | 明确问题已修复 | 新编辑使用 selectionProgressUpdatedAt / selectionProblemUpdatedAt；切换不重置已解决状态；旧共享时间仍作为兼容回退，不能据此声称已还原历史归属。recent 筛选只选有实际时间且不足3天的记录。 |
| B5 | 已修复 | 待立项真实状态；施工问题/说明、节点提醒、当前/剩余工作日、工期状态已补齐；节点提醒列黄色，工期红色预警与施工问题独立。 |
| U1/U2 | 已修复已测范围 | 手机显示铁塔名/阶段/问题/截止/状态；编辑网格自适应；节点时间线黄色；风险标签分开。保留原有紧凑风格。 |
| M1 | 部分修复 | 仅纠正快照 module_counts 的11/8摘要；75条记录及SEED完全未改。旧交接文档冲突保留并在本报告指出，未覆盖历史说明。 |
| M2 | 部分修复 | 删除不可达 renderSchedule 和 scheduleCols，保留旧URL跳转施工中；旧样式和大量preset暂保留。未拆分单文件或大规模重构。 |

### 验证结果

- 语法：对完整内联脚本使用 Node vm.Script 编译；git diff --check 通过。
- 42项独立函数/真实处理器回归：Asia/Shanghai、UTC、America/New_York 各42/42通过；包含跨DST的自然日计算。42是场景数，部分场景有多组日期断言。
- 8组浏览器检查通过：五模块数量和真实下载；编辑编码→施工→完工；正常/疑难反复切换和刷新保留；旧排程链接；大陆12个月日历；移动/平板/PC布局；正常路径无脚本异常；损坏缓存不覆盖。
- 浏览器布局覆盖360、390、768、1280宽度的七个模块和基础信息编辑；另有1440宽度截图。没有页面级横向溢出；手机导航有局部横向滚动，不挤压按钮。
- 实际下载5个XLSX并只读解析：18/19/18/36/16列，对应16/11/8/13/27行站点；ZIP CRC和XML结构有效；订单号为文本、日期为Excel日期；边框、微软雅黑、文本换行、冻结C4、打印标题、打印区域、A4横向、一页宽设置通过。
- 未启动本机Excel做打印预览，不能据XML设置声称36列施工表在A4纸上仍清晰可读。若要打印阅读，建议另行确认是否允许分组打印或增加纸张宽度；本轮保留用户要求的一页宽。
- 重复订单1226041515562086仍是两站，不被去重合并；华一路对应“肥西横排头路与将军岭中路交口东南”正确。
- 当前75个固定ID无重复；SEED与基线逐字相同；快照75条明细未改变；仅摘要12/7修为11/8。
- 没有真实生产浏览器缓存，因此旧缓存升级证据来自模拟历史版本标记与自定义字段。未声称覆盖所有用户历史状态。
- 初轮结果曾因D3未解决而不具备发布条件；D3后续已修复并补测，见末尾。尚未提交和部署，不代表生产已修复。

### 证据文件

- `audit-evidence/baseline-results.json`：原版本初轮28项16通过12失败，保留失败历史。
- `audit-evidence/unit-results.json`、`unit-results-utc.json`、`unit-results-new-york.json`：修复后42项。
- `audit-evidence/pending-migration-observations.json`：两个未解决迁移覆盖场景，reproduced=true。
- `audit-evidence/browser-results.json`、`export-results.json`：浏览器及XLSX结果。
- `audit-evidence/mobile-before.png`、`mobile-after.png`、`mobile-edit-after.png`、`desktop-after.png`：人工查看的布局截图。
- `tests/README.md`：复现命令、依赖与范围。

## 迁移机制建议（仅方案，未实施大规模重构）

1. 固定当前75站ID，保留原始缓存的外部JSON备份；浏览器内备份不是异机备份，清理站点数据仍会一起丢失。
2. 将每个已有preset登记为独立迁移：ID、目标字段、来源依据、适用旧版本、允许覆盖的确切旧值。先只登记并做dry-run差异报告，不改变当前执行链。
3. 基础补录只填空；明确纠错只覆盖列明的错误旧值；发现与旧值和新值都不同的人工值，保留并进入冲突清单。项目编码、完工、实际节点和站点分类不得靠统一的“新优先/旧优先”处理。
4. 每站一次匹配、每项一次迁移，建立完整字段级 before/after 证据；未知字段/历史跟进/暂停记录保留。验证成功后再写主KEY，失败保留原文。
5. 建立V0.8.41以来的代表性缓存fixture，以及用户自愿提供的脱敏真实缓存；验证初次迁移、重复执行、缺标记、部分执行、人工较新数据、配额失败和回滚。
6. 先替换一个边界明确的迁移，验收后再替换其余；不要同时重构页面、数据结构和全部迁移。

用户后续要求解决D3后，已采用保守可恢复的策略：“仅覆盖列明的已知错误旧值，其余冲突保留人工数据并列清单”。已确认的华一路错误塔名仍精确纠正。全面迁移登记与更多历史fixture仍是维护建议，不再让旧preset直接覆盖用户字段。

## 仍需确认的业务细节

- 旧文档前半部分与后续补充冲突：施工排程是否存在、自然日/工作日、节点逾期是否解除、12/7或11/8。此次执行以用户本轮明确要求为准；建议另行整理唯一现行规则，不删除历史记录。
- “超过3天”与现有“3天未更新”：现有判断是按日期差>=3，不是严格>3或满72小时。本轮保留原阈值；请确定是否需精确到小时。
- 截止后的非工作日：工作日超期数量应为0（已修正），但是否立即按自然日期显示“总工期逾期”需确认。
- 暂缓/取消的4个正常选址站仍保留 followupEnabled=false；切到疑难时不擅自启用提醒。是否需要人工切换时同时启用提醒，另行确认。

## 部署状态

2026-09-21复核生产部署 `dpl_BvvC617pATRQH7AhuHx19ZmZAxic` 为 READY，main提交仍为 `f277c7f...`，V0.8.63。本地补丁未提交、未推送、未部署，生产仍是原版。报告与代码在独立技术目录，未写入“一体”业务资料。

## D3闭环：历史preset不再静默覆盖人工数据

### 改动与策略

- `buildLegacyMigrationCandidate(input)` 将所有旧preset封装为私有函数，仅操作克隆副本，原有调用顺序保留。它们不能再从加载流程直接修改实际 state。
- `reconcileLegacyPresets` 使用独立版本 `20260921-v1`。既有字段包括空字符串、false、实际节点日期、完工标记都视为真实值；没有可靠依据时不覆盖。历史跟进、更新日志、暂停记录及未知字段保留，不把旧候选的描述性日志写成已经发生的新事实。
- 只有缺失字段可补默认值；明确纠错是固定ID+字段+精确错误旧值的白名单。本次白名单是华一路站错误塔名“肥西皋城路与周公山路交口西”→“肥西横排头路与将军岭中路交口东南”。若已有另一个新塔名则保留并记录，不猜测。
- 冲突保存为 migrationConflicts，页面顶部可展开查看站名、字段、保留值与候选值；migrationAudit 记录允许执行的变更。它们是升级时对照，用户后续编辑以详情页当前值为准。
- 合并底册也不再把用户明确保存的空值当成缺失字段。双侧唯一订单和固定ID的一对一规则不变。
- 在迁移前保存 `feixi-v072-state:before-preset-safety-v1` 原文备份；备份失败则停止，不能写主KEY。站点完整性校验和候选计算完成后，数据、冲突记录与版本标记在一次 setItem 中写入；失败不持久化完成标记。重复启动不会重放旧preset或重复累积冲突。
- 无缓存的新用户仍执行原有种子初始化链；现有SEED含部分历史字段，需要初始化后才是16/11/8/13/27。本轮没有修改SEED来凑数。

### 复现与验证

- `preset-safety-before.json`：新增专项初轮12项仅5通过，明确复现清空项目编码、覆盖进展/问题/日期等失败。
- `preset-safety-results.json`：修复后14/14通过，覆盖人工字段保留、已知纠错、未知塔名冲突、显式空值/false、历史数组、缺失/部分标记、重复执行、备份失败、主写失败、候选中断、75ID和重复订单隔离。
- 原42项回归通过，9组浏览器检查通过，5类实际XLSX导出再验证通过，语法和diff检查通过。
- 真实浏览器注入旧缓存后新项目编码保留，迁移冲突可展开查看，原文备份逐字一致；截图 `audit-evidence/migration-conflicts.png`。
- 未接触真实生产缓存。无法恢复用户在此前版本中已经被覆盖且无备份的数据；本修复防止此次及后续通过该加载路径的静默覆盖。检测到不确定业务值时保留并列明，不能把未知数据冲突自动视为已核实。
- **D3代码风险已闭环；修复仍在本地，未提交、未推送、未部署。** 其余待确认业务细节保持报告原状，不影响“不再静默覆盖”的结论。

## 2026-09-21 两项日常使用优化

用户最终仅保留跨日自动刷新、完整数据备份与恢复；不新增养护提醒、每日变化对比、施工待办细化、历史差异核对闭环。

- 日期每30秒及窗口重新获得焦点/可见时检查；列表自动重算。详情页保留编辑DOM并显示日期变化提示，保存或离开详情后更新计时。
- 下载完整JSON备份包含全部75站、历史数组、未知字段及全局设置。下载内容为已保存数据。
- 恢复前校验备份格式、当前版本、75站固定ID、历史数组及节点日期；显示字段差异，用户确认后完整替换。恢复前保存浏览器内副本；并发修改、预览过期或写入失败均中止，不显示成功。
- 目前支持本版本生成的完整备份，不接受任意JSON或旧版Excel作为恢复文件。
- 验证：原42项回归、14项迁移专项、9组浏览器测试通过；tests/daily-tools.cjs覆盖真实下载/预览/取消/恢复/重载、非法备份、过期预览、写入失败回滚、跨日、草稿保留及360px布局，均通过。git diff --check通过。
- GitHub远端查询本轮连接重置，未提交、推送或部署。本轮基于此前已核实的本地main副本及已有修复。

## V0.8.64 发布准备

用户已于2026-09-21明确授权提交部署。本轮远端fetch成功，确认main仍为f277c7f0d4d05ed3a10f93304bf2f7b3f54e002b。发布前42项回归、14项迁移、9组浏览器、完整备份与跨日专项、5份XLSX验证全部通过。此前“未提交部署”均为历史检查时状态。审计输出保留本地，测试脚本纳入版本控制。
